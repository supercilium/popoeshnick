import { Styles } from '@material-ui/core/styles/withStyles'
import { Theme } from '@material-ui/core/styles'

export const cardStyles: Styles<Theme, {}, 'card'> = ({
    card: {
        textAlign: 'left',
    },
})