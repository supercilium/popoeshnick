import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    root: {
        margin: '15px 0',
        color: '#bbb',
        textAlign: 'center',
        position: "absolute",
        bottom: 0,
        left: 0,
        right: 0,
    },
})